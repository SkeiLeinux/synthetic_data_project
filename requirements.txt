# Полный набор зависимостей для локальной разработки и запуска тестов из корня.
# В продакшн-билде каждый микросервис ставит только свой подмножественный
# services/<name>/requirements.txt — этот файл для тестов, IDE и CLI-инструментов.

# ─── Core ────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0

# ─── API / HTTP ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27

# ─── Validation / Config ─────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
PyYAML>=6.0

# ─── State store ─────────────────────────────────────────────
redis>=5.0

# ─── ProcessRegistry + import/export PostgreSQL ──────────────
SQLAlchemy>=2.0,<3.0
psycopg2-binary>=2.9

# ─── ML ──────────────────────────────────────────────────────
scikit-learn>=1.4.0

# ─── Deep learning ───────────────────────────────────────────
# Для CUDA-сборки устанавливать отдельно ДО этого файла:
#   pip install torch~=2.11.0 --index-url https://download.pytorch.org/whl/cu128
torch~=2.11.0
opacus~=0.14.0          # 0.15+ несовместим с SmartNoise 1.0.6

# ─── Синтезаторы ─────────────────────────────────────────────
smartnoise-synth==1.0.6
sdv~=1.35.1

# ─── Utils ───────────────────────────────────────────────────
tqdm>=4.67

# ─── Dev / Testing ───────────────────────────────────────────
pytest>=8.0
