# Reporting Service: вердикт PASS/FAIL/PARTIAL + запись JSON-отчёта.
# Минимальный образ — только FastAPI и Pydantic, без ML-зависимостей.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
